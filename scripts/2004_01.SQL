CREATE TABLE REG_FILES (
    CODIGO      INTEIRO_VALIDO,
    CODREGISTRO INTEIRO_VALIDO,
    NOME        STR60_VALIDO,
    TAMANHO     INTEIRO_PADRAO,
    DATA        TIMESTAMP,
    USUARIO     STR30,
    CONTEUDO    BLOB SUB_TYPE 0 SEGMENT SIZE 80,
    ORIGINAL    VARCHAR(255)
);

ALTER TABLE REG_FILES ADD CONSTRAINT REG_FILES_PK PRIMARY KEY (CODIGO);

ALTER TABLE REG_FILES ADD CONSTRAINT REG_FILES_FK01 FOREIGN KEY (CODREGISTRO) REFERENCES REGDIARIO (CODIGO) ON UPDATE CASCADE;

GRANT ALL ON REG_FILES TO "PUBLIC";